# Hello World RESTful Web Service

Run `mvn spring-boot:run`, then send a GET request to `http://localhost:8083/hello`.

Expected response: `Hello World!!`

In Chrome, open DevTools > Network, select the `/hello` request, and open **Headers**. In Postman, send the request and open the response **Headers** tab.
